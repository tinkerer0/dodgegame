# dodgegame

터미널에서 돌아가는 **턴제 닷지(turn-based dodge)** 생존 게임이다. 한 키 입력 = 한 턴 — 실시간 압박 없이 한 수씩 두면서, 사방 가장자리에서 스폰돼 직선으로 날아오는 장애물을 피해 얼마나 오래 버티는지 겨루는 회피 퍼즐이다.

## 조작법

이동은 방향키만 받는다 — hjkl도, 제자리 대기(`.`/SPACE)도 없다. 매 턴 반드시 방향 하나를 골라야 한다.

| 키 | 동작 |
|---|---|
| ← ↑ → ↓ (방향키) | 1칸 이동 (한 번 누르면 한 턴 진행) |
| `/` | 메뉴 열기 — 테마 / 나(플레이어) / 적(장애물) 3칸 (턴 소비 안 함) |
| `r` | 게임오버 후 재시작 |
| `q` | 종료 |

벽(격자 경계) 쪽으로 이동을 시도해도 그 턴 자체는 정상 소비된다 — 제자리에 머문 채 장애물은 그대로 전진한다. 이것이 사실상 유일한 "머무르기" 수단이다.

## 규칙

- 그리드 경계는 **벽** — 플레이어는 격자 밖으로 못 나간다.
- 장애물은 가장자리에서 스폰돼 **직선으로만** 이동한다(호밍 없음). 격자 밖으로 나가면 사라진다.
- **텔레그래프**가 항상 켜져 있다 — 각 장애물의 다음 턴 위치가 흐린 `·`로 미리 보인다(텔레그래프 글리프는 스킨 대상이 아니라 항상 고정).
- 장애물이 플레이어 칸에 들어오면 게임오버. **스쳐 지나가며 자리를 맞바꾸는 경우**(플레이어와 장애물이 서로의 칸으로 동시에 이동)도 충돌로 처리한다.
- 점수 = 생존한 턴 수(HUD에는 점수만 표시 — 턴 수와 항상 같은 값이라 따로 안 보여준다). 턴이 지날수록 장애물 스폰 확률·개수가 점점 늘어난다(단, 회피 자체가 불가능해지지 않도록 상한을 두고, 스폰 직후 다음 턴에 곧바로 플레이어를 덮치는 즉사 조합은 재시도로 피한다). 이전 버전보다 난이도가 소폭 상향됐다.

## 메뉴 (테마 / 나 / 적)

`/`를 누르면 화면 중앙에 3칸짜리 메뉴가 뜬다 — 왼쪽부터 **테마 / 나(플레이어) / 적(장애물)**. 화면에는 각각 `Theme` / `You` / `Enemy`로 표시된다(UI 문구는 영어).

| 키 | 동작 |
|---|---|
| `←` `→` | 칸 이동 (테마 ↔ 나 ↔ 적, 순환) |
| `↑` `↓` | 활성 칸 안에서 후보 이동 |
| `Enter` | 확정 |
| `ESC` / `/` / `q` | 취소(메뉴 열기 전 상태로 복구) — 칸과 무관하게 항상 동작 |
| (나·적 칸에서) 인쇄 가능한 문자 키 | 그 칸의 글리프를 즉시 그 문자로 지정("직접 입력", 후보 목록에 없어도 가능) |

커서가 가리키는 테마/글리프는 배경 게임 화면에 실시간으로 미리보기되고, 취소하면 원래 설정으로 되돌아간다. 메뉴가 열려있는 동안은 게임 턴이 절대 진행되지 않고, 게임오버 화면에서도 열 수 있다.

테마가 10종이라 작은 터미널에서는 메뉴 박스가 화면보다 커질 수 있다 — 그럴 땐 헤더/힌트 줄은 고정한 채 목록만 스크롤돼 커서가 항상 화면 안에 보이고, 위/아래가 잘렸으면 `^`/`v` 마커로 알려준다.

### 테마 (색만 결정 — 10종)

256색 이상을 지원하는 터미널(`TERM=xterm-256color` 등)에서는 확장 팔레트로,
8색만 지원하는 터미널에서는 자동으로 표준 8색 폴백으로 그려진다 — 런타임에
`curses.COLORS`를 보고 알아서 고른다(설정 불필요). 8색 폴백 상황에서는 일부
신규 테마가 서로 비슷해 보일 수 있는데, 이건 8색의 물리적 한계로 의도적으로
허용한다(크래시 없이 동작하는 게 우선). 기존 5종(Classic~Ocean)의 색 구성은
이번 확장으로 전혀 바뀌지 않았다.

| # | 이름 | 느낌 |
|---|---|---|
| 1 | Classic | 기본값 — 초록 플레이어 / 빨강 적 / 노랑 흐린 텔레그래프 |
| 2 | Mono | 흑백 — 색 없이 굵기/흐림만으로 구분 |
| 3 | Neon | 마젠타 + 시안 대비 |
| 4 | Amber | 호박색 단색(굵기/흐림으로 계조) |
| 5 | Ocean | 파랑/시안/흰색 |
| 6 | Forest | 짙은 숲 — 라임그린 플레이어 / 흙갈색 적 |
| 7 | Sunset | 노을 — 주황 플레이어 / 진분홍 적 |
| 8 | Ice | 얼음 — 하늘색 플레이어 / 서리 흰색 적 |
| 9 | Toxic | 형광 — 라임그린 플레이어 / 전기보라 적(Neon과 안 겹치는 계열) |
| 10 | Crimson | 진홍 — 은회색 플레이어 / 짙은 진홍 적 |

### 스킨 (글리프만 결정 — 나/적 각각 별도 선택)

테마와 스킨은 서로 독립이다 — 어떤 테마를 골라도 나/적 글리프는 별도로 지정한다.

| 칸 | 기본값 | 후보 |
|---|---|---|
| 나(플레이어) | `@` | `@` `O` `0` `&` `A` `%` `8` |
| 적(장애물) | `*` | `*` `#` `x` `o` `^` `+` `!` |

**테마·스킨은 저장되지 않는다** — 껐다 켜면 항상 기본값(Classic 테마 + `@`/`*`)으로 돌아간다. 설정 파일 없음.

## 설치 (심링크)

```bash
ln -sf "$(pwd)/dodgegame.py" ~/.local/bin/dodgegame
```

레포를 clone한 폴더 안에서 위 명령을 실행하면 된다. `~/.local/bin`이 PATH에 잡혀 있으면 이후 터미널 어디서든 `dodgegame`로 바로 실행된다.

## 실행법

```bash
python3 dodgegame.py
```

또는 심링크를 걸었다면:

```bash
dodgegame
```

터미널은 최소 24열 x 12행 이상이어야 하며, 너무 작으면 크래시 대신 크기를 키워달라는 안내 메시지가 뜬다.

## 자기테스트

curses 화면을 띄우지 않고 게임 로직만 검증하는 헤드리스 모드가 있다.

```bash
python3 dodgegame.py --selftest
```

직접 충돌, 정상 진행 시 턴/점수 일치, 벽 경계에서의 좌표 불변, 스쳐 지나가는 pass-through 충돌 탐지, 격자 이탈 장애물 제거, 시드 고정 시 결정적 재현, 2000턴 랜덤 입력(게임오버 시 재시작 포함, 방향키 4종만) 무예외 완주, 재시작 시 상태 초기화, `step()`이 4방향 action만 허용(그 외엔 KeyError + 상태 불변), 테마 카탈로그 10종 + 색 필드 완비(글리프 필드 없음), 기존 5개 테마 색 구성 불변, 테마 10종 전부 (플레이어,적) 확장색 조합 유일성, 테마 이름 유일성 + 10자 이내, 8색 폴백 값 범위(0~7) 검증, 메뉴 커서 순환/클램프 경계 안전, 나/적 스킨 후보 목록(단일폭 ASCII + 기본값), 3칸 메뉴 좌우 칸 이동, 3칸 메뉴 칸 안 커서 이동(직접입력 경계 포함), 나/적 칸 자유 문자 입력 해석(인쇄 가능 ASCII만 허용·테마 칸 무시·제어문자/방향키 거부), 3칸 메뉴 확정/취소, 메뉴 스크롤 창 계산(목록이 창보다 짧으면 스크롤 없음 + 커서가 위/아래/가운데 어디든 항상 창 안에 보임), 활성 칸별 행 색인 계산까지 23개 불변식을 검증하고 마지막 줄에 `SELFTEST PASS` 또는 `SELFTEST FAIL: <이유>`를 출력한다(실패 시 종료 코드도 0이 아니다).

`python3 dodgegame.py --help`는 화면을 띄우지 않고 사용법 요약만 출력한다.

## 의존성

Python 3 표준 라이브러리만 사용한다. 유일하게 눈에 띄는 import는 `curses`이며, pip으로 따로 설치할 패키지는 없다.

터미널 게임이라 받아서 돌리기 전에 뭘 하는 프로그램인지 궁금할 수 있는데, 이 게임은 **파일을 만들거나 쓰지 않고**(점수·설정 저장 없음), **네트워크를 쓰지 않으며**, 외부 프로세스를 실행하지 않는다. 하는 일은 키 입력을 받아 화면을 그리는 것뿐이다.

## 기여·피드백

제가 이런 분야를 접한 지 얼마 안 돼서 부족한 점이 많습니다. 고칠 점이나 알려주실 내용이 있다면 issue나 PR로 남겨주시면 너무 감사하겠습니다.

## 라이선스

MIT © tinkerer0
